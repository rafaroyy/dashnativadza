"use client"

export default function DigitalzLogo({ className }: { className?: string }) {
  return <span className={`font-bold text-xl tracking-tight text-primary ${className ?? ""}`}>{"Digitalz"}</span>
}
